cmake_minimum_required(VERSION 3.8)
PROJECT(EXERCISE1)
SET(CMAKE_CXX_STANDARD 11)

FIND_PACKAGE(EIGEN3 REQUIRED)
#FIND_LIBRARY(EIGEN_LIBRARY NAMES EIGEN3)
Find_library(freeimag_library NAMES FREEIMAGE)
INCLUDE_DIRECTORIES(${EIGEN3_INCLUDE_DIRS} ${FREEIMAGE_INCLUDE_DIRS})
LINK_DIRECTORIES(${EIGEN3_LIBRARY_DIRS} ${FREEIMAGE_LIBRARY_DIRS})

ADD_EXECUTABLE(mytest main.cpp FreeImageHelper.h FreeImageHelper.cpp VirtualSensor.h)
TARGET_LINK_LIBRARIES(mytest freeimage ${Eigen3_LIBRARIES})
